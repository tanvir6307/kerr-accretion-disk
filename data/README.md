# Data Directory

- `external/`: immutable third-party or archived inputs with provenance.
- `interim/`: intermediate products that can be regenerated.
- `processed/`: versioned outputs used by figures, tables, or manuscripts.

Large generated products should not be committed unless explicitly required and
documented with checksums.
